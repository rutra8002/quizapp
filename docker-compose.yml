services:
  quizapp:
    build: .
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: dev-secret-key
      GEMINI_API_KEY: ${GEMINI_API_KEY}
    volumes:
      - ./instance:/app/instance